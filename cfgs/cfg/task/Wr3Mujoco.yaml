defaults:
  - Wr3Terrain.yaml

name: Wr3Mujoco

record:
  record_video: False
  video_save_path: './runs/record.mp4'

env:
  numEnvs: 1

  control:
    # PD Drive parameters:
    stiffness: 80.  # [N*m/rad]
    damping: 2.     # [N*m*s/rad]